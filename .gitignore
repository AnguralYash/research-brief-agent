# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Generated run artifacts — but DO keep the committed samples.
outputs/*
!outputs/sample_brief.md
!outputs/sample_brief.json

# Local cache of fetched HTML + LLM responses (regenerated on demand)
.cache/
