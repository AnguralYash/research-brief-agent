"""Research-and-Summarize Agent.

A multi-stage agent that fetches a small set of source URLs, extracts atomic
claims from each, reasons across them to surface consensus / contradictions /
outliers / gaps, and renders a cited analyst brief (Markdown + JSON).
"""

__version__ = "0.1.0"
